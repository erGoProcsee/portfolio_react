

import SectionWrapper from "./SectionWrapper";

export {SectionWrapper}

// Higher-Order componets 
/*
    
    It’s a way to share common functionality across multiple
    components without repeating code.

    a pattern in React that allows reuse of
    component logic. An HOC is a function that wraps
    aroud a component and returns a new component with
    additional props or behavior.
    
    allows for the returning of another function
    
*/